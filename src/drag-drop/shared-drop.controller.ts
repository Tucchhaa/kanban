import { BaseController } from "../base/controller";

export class SharedDropController extends BaseController {
    constructor() {
        super();

        // this.eventEmitter.on('process-drag')
    }
}